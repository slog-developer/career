export default function Filter() {
  /**
   *
   * development filter plan
   * 1. 첫번째 필터는 가격을 정하는 필터를 만든다.
   * - 맨위에는 라디오 버튼을 구성해서 라디오 그룹은 : {전체, 10만원 이하, 10-30만원, 30-50만원, 50-100만원, 100-200만원, 200-300만원, 300-400만원}
   * - 가격의 최소값가 최대값의 Range를 선택할수 있는 하나의 슬라이더를 만든다.
   * -
   */

  return <div>Filter Component</div>;
}
